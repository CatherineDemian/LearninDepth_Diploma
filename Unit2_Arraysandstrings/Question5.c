/*
 * Question5.c
 *
 *  Created on: Jul 29, 2023
 *      Author: Catherine Nader
 */
/*
#include"stdio.h"

int main()
{

	int n,numsearch;
	int elements[30];
	printf("Enter no of elements: ");
	fflush(stdin); fflush(stdout);
	scanf("%d\n",&n);

	for(int i=0;i<n;i++)
	{
		scanf("%d",&elements[i]);

	}

	printf("Enter the element to be searched: ");
	fflush(stdin); fflush(stdout);
	scanf("%d",&numsearch);

	for(int i=0;i<n;i++)
	{

		if(elements[i]==numsearch)
		{
			printf("\nNumber found at the location=%d",i+1);
		}



	}
	return 0;
}
*/
